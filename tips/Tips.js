// 左に0を埋める
const zeroPadding = (NUM, LEN) => (Array(LEN).join('0') + NUM).slice(-LEN);

// 数値を二進数に変換
var binary = num.toString(2);

// 数値をn進数として認識する
var num = parseInt(str, baseNum);

// 改行なし出力
process.stdout.write(")")
